version: '3.1'

services:
  db:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: laaila_reservas
      POSTGRES_USER: laaila_reservas
      POSTGRES_PASSWORD: laaila_reservas